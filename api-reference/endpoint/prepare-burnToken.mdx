---
title: 'burnToken'
openapi: 'POST /prepare-transactions/burnToken'
---

Prepares unsigned transactions for burning tokens from the signer's balance. This endpoint is equivalent to calling `/prepare-transactions` with `method=burnToken`.

## Request Body

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `signerAddress` | string | Yes | Address that will sign the transaction |
| `chainId` | string | Yes | Blockchain network identifier (hex format) |
| `method` | string | Yes | Transaction method type (should be "burnToken") |
| `tokenSymbol` | string | Yes | Symbol of the token to burn |
| `amount` | string | Yes | Amount of tokens to burn |
| `investorEmail` | string | Yes | Email of the investor burning tokens |

## Example Request

```json
{
  "signerAddress": "0x742d35Cc6634C0532925a3b8D4C9db96c4b4d8b",
  "chainId": "aa36a7",
  "method": "burnToken",
  "tokenSymbol": "PROP1",
  "amount": "100",
  "investorEmail": "investor@example.com"
}
```

## Response

Returns an array of unsigned transactions that need to be signed and a transaction ID for tracking.

```json
{
  "transactions": [
    {
      "from": "0x742d35Cc6634C0532925a3b8D4C9db96c4b4d8b",
      "to": "0xTargetAddress",
      "value": {"type": "BigNumber", "hex": "0x00"},
      "nonce": 1,
      "chainId": 11155111,
      "data": "0x...",
      "type": 2,
      "maxPriorityFeePerGas": {"type": "BigNumber", "hex": "0x..."},
      "maxFeePerGas": {"type": "BigNumber", "hex": "0x..."},
      "gasLimit": {"type": "BigNumber", "hex": "0x..."}
    }
  ],
  "txId": "unique-transaction-id"
}
```