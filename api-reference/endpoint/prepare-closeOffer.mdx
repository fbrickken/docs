---
title: 'closeOffer'
openapi: 'POST /prepare-transactions'
---

import NextSignAndSend from '/snippets/next-sign-and-send.mdx';

Prepares unsigned transactions for closing an active Security Token Offering (STO). Use `POST /prepare-transactions` with `method=closeOffer`.

## Notes

- Only the tokenizer who created the STO can close it
- Closing an STO before the end date will make it unavailable for new investments
- Existing investments remain valid and tokens can still be claimed
- This action is irreversible once the transaction is confirmed

<NextSignAndSend />
