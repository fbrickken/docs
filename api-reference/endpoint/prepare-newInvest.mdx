---
title: 'newInvest'
openapi: '/api-reference/prepare/newInvest.json POST /prepare-transactions'
---

import NextSignAndSend from '/snippets/next-sign-and-send.mdx';

Prepares unsigned transactions for making an investment in a Security Token Offering (STO). Use `POST /prepare-transactions` with `method=newInvest`.

## Notes

- The investor must have sufficient balance of the payment token
- Investment amount must be within the STO's min/max investment limits
- The STO must be active and within the investment period
- The investor address will be automatically whitelisted if required

<NextSignAndSend />
